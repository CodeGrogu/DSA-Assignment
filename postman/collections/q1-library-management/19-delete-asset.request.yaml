$kind: http-request
name: 19. Delete Asset
method: DELETE
url: "{{base_url}}:{{port}}/assets/{{assetTag}}"
headers:
  - key: Accept
    value: application/json
description: |-
  ### Endpoint Overview
  Permanently removes an asset record and all attached sub-resources from the system store as final teardown.

  ---
  ### HTTP Specification
  - **Method**: `DELETE`
  - **URL**: `{{base_url}}:{{port}}/assets/{{assetTag}}`
  - **Headers**:
    - `Accept`: `application/json`

  ---
  ### Automated Test Assertions
  1. **Status Code Check**: Asserts HTTP `200 OK` or `204 No Content`.
order: 19000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 200 or 204", function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 204]);
      });
